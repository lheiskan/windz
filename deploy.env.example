# WindZ Monitor Deployment Configuration
# Copy this file to deploy.env and configure for your environment
#
# NOTE: The deployment user must have passwordless sudo access for:
# - systemctl commands (stop/start/is-active service)
# - file operations (cp, chown, chmod in /opt directories)
# - journalctl for log access
#
# Example sudoers entry:
# deploy ALL=(ALL) NOPASSWD: /bin/systemctl, /bin/cp, /usr/bin/chown, /bin/chmod, /bin/journalctl

# Target server hostname or IP
DEPLOY_HOST=your.server.com

# SSH user for deployment
DEPLOY_USER=deploy

# Service name (systemd unit)
DEPLOY_SERVICE_NAME=windz

# Target installation path
DEPLOY_TARGET_PATH=/opt/windz/windz

# Service user for ownership
DEPLOY_SERVICE_USER=windz

# Optional: Custom SSH port (default: 22)
# DEPLOY_SSH_PORT=2222

# Optional: Binary name (default: windz)
# DEPLOY_BINARY=windz

# Health check URL (default: http://localhost:8080/health)
DEPLOY_HEALTH_URL=http://localhost:8081/health